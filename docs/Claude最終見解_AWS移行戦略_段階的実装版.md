# 「Fulfillment by THE WORLD DOOR」AWS段階的実装計画

**作成日**: 2025年10月23日

---

### 核心的方針

1. **フェーズ1（MVP）**: 最小構成でローンチ、運用を学ぶ
2. **フェーズ2（スケール）**: 性能を強化、運用を安定化
3. **フェーズ3（エンタープライズ）**: セキュリティ・コンプライアンスを完備
4. **Redis不要**: Socket.io未実装のためフェーズ1ではRedis省略

### 重要な前提

- **技術選定は不変**: ECS Fargate + Aurora MySQL Serverless v2 の選択は全フェーズ共通
- **データベース**: MySQL（担当エンジニアの習熟度を優先）
- **段階的拡張**: 最初から全部盛りせず、必要に応じて機能追加
- **コスト最適化**: 各フェーズで必要最小限のリソースで開始
- **状態監視**: ポーリング方式（30秒間隔）でフェーズ1実装

---

## 🎯 フェーズ1: MVP（ミニマムローンチ）

### ビジネス要件

```
セラー数: 10名（身内のみ）
物流件数: 50件/日
商品数: 約1,000点
DAU: 10-15人
セキュリティ: 最低限（内部利用のみ）
```

### アーキテクチャ構成

```
Internet
  ↓
[Route 53] 独自ドメイン管理
  ↓
[ALB] Multi-AZ ← 可用性優先
  ↓
[ECS Fargate Cluster]
  ├─ タスク数: 2タスク（Multi-AZ配置）
  ├─ スペック: 0.25vCPU, 0.5GB
  ├─ ポーリング方式: 30秒間隔
  └─ デプロイ: ローリングアップデート
  ↓
[RDS Proxy]
  ↓
[Aurora Serverless v2 (MySQL)]
  ├─ 0.5 ACU固定（スケーリング無効）
  ├─ Multi-AZ（可用性向上）
  └─ バックアップ保持: 7日

[S3]
  ├─ バケット: 1個（画像/PDF/動画）
  ├─ CDN: なし（直接アクセス）
  └─ ライフサイクル: 90日後Glacier移行

セッション管理:
└─ JWT（ステートレス、Redis不要）

横断サービス:
├─ Secrets Manager（DB認証のみ）
├─ CloudWatch Logs（ログ保持7日）
└─ EventBridge（なし、cron不要）
```

### サービス選定（フェーズ1）

| 層 | サービス | 仕様 | 判断理由 |
|---|---|---|---|
| **DNS** | Route 53 | 1ホストゾーン | ✅ 標準 |
| **CDN/WAF** | なし | - | ❌ コスト削減（S3直接アクセス） |
| **LB** | ALB | Multi-AZ | ✅ 可用性優先 |
| **Compute** | ECS Fargate | 2タスク, 0.25vCPU/0.5GB | ✅ Multi-AZ配置 |
| **Container** | ECR | 1リポジトリ | ✅ 標準 |
| **Database** | Aurora Serverless v2 (MySQL) | 0.5 ACU固定, Multi-AZ | ✅ 可用性優先 |
| **DB Proxy** | RDS Proxy | 1エンドポイント | ✅ 必須（接続プーリング） |
| **Cache** | なし | - | ❌ フェーズ1不要 |
| **Storage** | S3 | 1バケット, CDNなし | ✅ 標準 |
| **Secrets** | Secrets Manager | 2シークレット（DB/JWT） | ✅ 必須 |
| **Monitoring** | CloudWatch Logs | 7日保持 | ✅ 最低限 |
| **Backup** | Aurora自動バックアップ | 7日保持 | ✅ Multi-AZ対応 |
| **IaC** | AWS CDK (TypeScript) | - | ✅ 標準 |

### コスト試算（フェーズ1）

| サービス | 仕様 | 月額（USD） | 月額（JPY）* |
|---------|-----|-----------|------------|
| **ECS Fargate** | 0.25vCPU, 0.5GB × 2タスク × 730h | $18 | ¥2,700 |
| **ALB** | 1個（Multi-AZ）、LCU 5/h | $25 | ¥3,750 |
| **Aurora Serverless v2 (MySQL)** | 0.5 ACU × 730h, Multi-AZ | $88 | ¥13,200 |
| **RDS Proxy** | 1エンドポイント × 730h | $11 | ¥1,650 |
| **ElastiCache Redis** | フェーズ1不要 | $0 | ¥0 |
| **S3** | 20GB + 転送10GB | $2 | ¥300 |
| **ECR** | 5GB | $0.5 | ¥75 |
| **Route 53** | 1ホストゾーン + 10万クエリ | $1 | ¥150 |
| **Secrets Manager** | 2シークレット | $0.8 | ¥120 |
| **CloudWatch Logs** | 2GB（7日保持） | $1 | ¥150 |
| **データ転送** | NAT Gateway 10GB | $1 | ¥150 |
| **合計** | | **$147.5** | **¥22,125** |

*1 USD = 150 JPYで換算

### フェーズ1の重要なポイント

**コスト削減:**
1. **Redis不要**: Socket.io未実装（-¥1,500）
2. **CDN無し**: CloudFront不使用（S3直接アクセス）
3. **オートスケール無し**: 固定2タスク
4. **監視最小限**: CloudWatch Logsのみ（X-Ray/Sentryなし）

**可用性優先:**
1. **Multi-AZ有効**: Aurora + ALB（+¥7,950）
2. **2タスク配置**: 可用性向上
3. **バックアップ7日**: PITR対応

### フェーズ1で削除しないもの（必須コンポーネント）

1. **RDS Proxy**: 接続数管理に必須
2. ~~**ElastiCache Redis**~~: フェーズ1不要（Socket.io未実装）
3. **Aurora Serverless v2 (MySQL)**: Prisma互換性に必須
4. **ECS Fargate**: 柔軟性・拡張性に必須

---

## 🚀 フェーズ2: スケール（性能強化）

### ビジネス要件

```
セラー数: 30名（まだ身内）
物流件数: 150件/日（3倍）
商品数: 約3,000点
DAU: 30-40人
セキュリティ: 最低限（内部利用のみ）
```

### アーキテクチャ変更点（フェーズ1からの差分）

```
[ALB] Multi-AZ ← 可用性向上
  ↓
[ECS Fargate Cluster]
  ├─ タスク数: 2-6タスク（オートスケール有効）← 性能向上
  ├─ スペック: 0.5vCPU, 1GB ← リソース増強
  └─ ヘルスチェック: 詳細化
  ↓          ↓
[RDS Proxy] [ElastiCache Redis]
  ↓          ├─ cache.t4g.small × 2（Multi-AZ）← 可用性向上
  ↓          └─ レプリケーション有効
  ↓
[Aurora Serverless v2]
  ├─ 0.5-2 ACU（オートスケール有効）← 性能向上
  ├─ Multi-AZ ← 可用性向上
  └─ バックアップ保持: 7日

[S3 + CloudFront] ← CDN追加（性能向上）
  ├─ CloudFront配信
  ├─ キャッシュ設定
  └─ Gzip圧縮

新規追加:
├─ CloudWatch Alarms（10個）← 監視強化
├─ X-Ray（トレーシング）← パフォーマンス分析
└─ AWS Backup（週次フルバックアップ）← データ保護強化
```

### サービス変更点（フェーズ2）

| サービス | フェーズ1 | フェーズ2 | 変更理由 |
|---------|----------|----------|---------|
| **ALB** | Multi-AZ | Multi-AZ | 変更なし |
| **ECS Fargate** | 2タスク固定 | 2-6タスク（オートスケール） | 性能向上 |
| **ECS スペック** | 0.25vCPU/0.5GB | 0.5vCPU/1GB | 処理能力向上 |
| **Aurora** | 0.5 ACU固定, Multi-AZ | 0.5-2 ACU, Multi-AZ | 性能向上 |
| **Redis** | なし | Socket.io実装時に追加 | 必要時のみ |
| **CDN** | なし | CloudFront | 性能向上 |
| **監視** | Logsのみ | Logs + Alarms + X-Ray | 運用品質向上 |
| **バックアップ** | 7日保持 | 7日保持 + 週次フル | データ保護強化 |

### コスト試算（フェーズ2）

| サービス | 仕様 | 月額（USD） | 月額（JPY）* | 差分 |
|---------|-----|-----------|------------|------|
| **ECS Fargate** | 0.5vCPU, 1GB × 平均3タスク | $106 | ¥15,900 | +¥13,200 |
| **ALB** | Multi-AZ、LCU 10/h | $25 | ¥3,750 | ±0 |
| **Aurora Serverless v2 (MySQL)** | 0.5-2 ACU平均1 ACU, Multi-AZ | $88 | ¥13,200 | ±0 |
| **RDS Proxy** | 2エンドポイント | $22 | ¥3,300 | +¥1,650 |
| **ElastiCache Redis** | なし（Socket.io時追加） | $0 | ¥0 | ±0 |
| **S3 + 転送** | 50GB + 転送30GB | $5 | ¥750 | +¥450 |
| **CloudFront** | 100GB転送 + 100万リクエスト | $10 | ¥1,500 | +¥1,500 |
| **ECR** | 10GB | $1 | ¥150 | +¥75 |
| **Route 53** | 1ホストゾーン + 50万クエリ | $1 | ¥150 | ±0 |
| **Secrets Manager** | 3シークレット | $1.2 | ¥180 | +¥60 |
| **CloudWatch** | Logs 5GB + Alarms 10個 + Metrics | $36 | ¥5,400 | +¥5,250 |
| **X-Ray** | 5万トレース | $0.25 | ¥38 | +¥38 |
| **AWS Backup** | 30GB × $0.05 | $1.5 | ¥225 | +¥75 |
| **データ転送** | NAT Gateway 30GB | $2 | ¥300 | +¥150 |
| **合計** | | **$299** | **¥44,850** | **+¥22,725** |

*1 USD = 150 JPYで換算

### フェーズ2への移行判断基準

以下の条件を**2つ以上**満たしたらフェーズ2へ移行:

1. **セラー数**: 20名以上
2. **物流件数**: 100件/日以上
3. **レスポンス悪化**: P95レスポンスタイムが3秒超過
4. **CPU使用率**: 平均70%以上を3日間継続
5. **ダウンタイム発生**: 月1回以上のサービス停止
6. **リアルタイム通知需要**: ポーリングでは不足する要件が発生

### フェーズ2で実施する作業

1. **リアルタイム通知検討** (所要時間: 1日)
   - Socket.io実装判断
   - Redis追加検討（必要時のみ）

2. **オートスケール設定** (所要時間: 4時間)
   - ECS Service Auto Scaling設定
   - Aurora Serverless v2スケーリング設定
   - CloudWatch Alarms設定

3. **CDN導入** (所要時間: 3時間)
   - CloudFront Distribution作成
   - S3バケットポリシー変更
   - キャッシュ設定最適化

4. **監視強化** (所要時間: 2時間)
   - X-Ray統合
   - CloudWatch Dashboards作成
   - アラート通知設定

**合計移行時間: 約10時間（1-2日間）**

---

## 🏢 フェーズ3: エンタープライズ（本格運用）

### ビジネス要件

```
セラー数: 100名（対外サービス開始）
物流件数: 300件/日
商品数: 約10,000点
DAU: 80-100人
セキュリティ: 本格対応（コンプライアンス必須）
```

### アーキテクチャ変更点（フェーズ2からの差分）

```
Internet
  ↓
[Route 53] + [Route 53 Health Checks] ← 監視強化
  ↓
[AWS WAF] ← セキュリティ追加
  ↓
[CloudFront]
  ├─ AWS Shield Standard（DDoS対策）← セキュリティ追加
  ├─ Lambda@Edge（リクエスト制御）← セキュリティ追加
  └─ アクセスログ（S3保存）← コンプライアンス
  ↓
[ALB] Multi-AZ
  ├─ アクセスログ有効化 ← コンプライアンス
  └─ TLS 1.3必須 ← セキュリティ強化
  ↓
[ECS Fargate Cluster]
  ├─ タスク数: 4-10タスク
  ├─ スペック: 1vCPU, 2GB ← さらに増強
  └─ タスクロールによる最小権限 ← セキュリティ強化
  ↓          ↓
[RDS Proxy] [ElastiCache Redis]
  ↓          ├─ cache.t4g.medium × 2（Multi-AZ）
  ↓          └─ 暗号化有効 ← セキュリティ強化
  ↓
[Aurora Serverless v2]
  ├─ 1-4 ACU（オートスケール）
  ├─ Multi-AZ
  ├─ 暗号化有効（KMS） ← セキュリティ強化
  ├─ バックアップ保持: 30日 ← コンプライアンス
  └─ Performance Insights有効 ← 運用品質

[S3]
  ├─ バージョニング有効 ← データ保護
  ├─ 暗号化（SSE-S3） ← セキュリティ強化
  ├─ アクセスログ ← コンプライアンス
  └─ ライフサイクル: 30日後IA、180日後Glacier

新規追加（セキュリティ・コンプライアンス）:
├─ AWS Config（リソース監査）
├─ CloudTrail（APIログ記録）
├─ GuardDuty（脅威検知）
├─ Security Hub（セキュリティ統合管理）
├─ VPC Flow Logs（ネットワーク監視）
├─ Sentry（エラー追跡）← 運用品質
└─ Cognito（将来的な認証統合検討）
```

### サービス変更点（フェーズ3）

| サービス | フェーズ2 | フェーズ3 | 変更理由 |
|---------|----------|----------|---------|
| **WAF** | なし | AWS WAF | セキュリティ（SQL/XSS対策） |
| **Shield** | なし | Standard | DDoS対策 |
| **ECS スペック** | 0.5vCPU/1GB | 1vCPU/2GB | 処理能力向上 |
| **ECS タスク数** | 2-6 | 4-10 | スケール対応 |
| **Aurora** | 0.5-2 ACU | 1-4 ACU | 性能向上 |
| **Aurora暗号化** | なし | KMS暗号化 | セキュリティ |
| **バックアップ** | 7日保持 | 30日保持 + 月次 | コンプライアンス |
| **Redis** | なし | Socket.io時にt4g.medium追加 | 必要時のみ |
| **S3** | 標準 | バージョニング + 暗号化 | データ保護 |
| **監査** | なし | Config + CloudTrail | コンプライアンス |
| **脅威検知** | なし | GuardDuty | セキュリティ |
| **エラー追跡** | なし | Sentry | 運用品質 |

### コスト試算（フェーズ3）

| サービス | 仕様 | 月額（USD） | 月額（JPY）* | 差分 |
|---------|-----|-----------|------------|------|
| **ECS Fargate** | 1vCPU, 2GB × 平均6タスク | $284 | ¥42,600 | +¥26,700 |
| **ALB** | Multi-AZ、LCU 15/h、ログ有効 | $30 | ¥4,500 | +¥750 |
| **Aurora Serverless v2 (MySQL)** | 1-4 ACU平均2 ACU, Multi-AZ, 暗号化 | $176 | ¥26,400 | +¥13,200 |
| **RDS Proxy** | 2エンドポイント | $22 | ¥3,300 | ±0 |
| **ElastiCache Redis** | Socket.io時にt4g.medium × 2追加 | $0-80 | ¥0-12,000 | +¥0-12,000 |
| **S3 + 転送** | 100GB + 転送50GB | $7 | ¥1,050 | +¥300 |
| **CloudFront** | 500GB転送 + 500万リクエスト | $46 | ¥6,900 | +¥5,400 |
| **AWS WAF** | 1 Web ACL + 10ルール | $11 | ¥1,650 | +¥1,650 |
| **ECR** | 10GB | $1 | ¥150 | ±0 |
| **Route 53** | Health Checks 3個 + 100万クエリ | $2.5 | ¥375 | +¥225 |
| **Secrets Manager** | 5シークレット | $2 | ¥300 | +¥120 |
| **CloudWatch** | Logs 10GB + Alarms 20個 + Metrics + Dashboard | $50 | ¥7,500 | +¥2,100 |
| **X-Ray** | 10万トレース | $0.5 | ¥75 | +¥37 |
| **AWS Backup** | 50GB スナップショット | $2.5 | ¥375 | +¥150 |
| **CloudTrail** | 1証跡 + S3ストレージ | $2 | ¥300 | +¥300 |
| **AWS Config** | 10リソース記録 | $2 | ¥300 | +¥300 |
| **GuardDuty** | VPC Flow + CloudTrail分析 | $10 | ¥1,500 | +¥1,500 |
| **Security Hub** | 1アカウント | $1 | ¥150 | +¥150 |
| **VPC Flow Logs** | 20GB/月 | $1 | ¥150 | +¥150 |
| **Sentry** | 50K events/月（外部SaaS） | $26 | ¥3,900 | +¥3,900 |
| **データ転送** | NAT Gateway 50GB | $2.25 | ¥338 | +¥38 |
| **合計（Redis無し）** | | **$679** | **¥101,850** | **+¥57,000** |
| **合計（Redis追加時）** | | **$759** | **¥113,850** | **+¥69,000** |

*1 USD = 150 JPYで換算

### フェーズ3への移行判断基準

以下の条件を**3つ以上**満たしたらフェーズ3へ移行:

1. **セラー数**: 50名以上
2. **物流件数**: 200件/日以上
3. **外部セラー開始**: 社外のセラーを受け入れる計画
4. **コンプライアンス要求**: 顧客からセキュリティ証明を求められる
5. **SLA必要**: 稼働率99.5%以上を保証する必要がある
6. **データ保護強化**: 個人情報保護法対応が必要

### フェーズ3で実施する作業

1. **セキュリティ強化** (所要時間: 8時間)
   - AWS WAF設定（SQLインジェクション、XSS対策）
   - Aurora/Redis暗号化有効化
   - S3バージョニング + 暗号化
   - IAMロール最小権限化

2. **コンプライアンス対応** (所要時間: 6時間)
   - CloudTrail有効化（全APIログ記録）
   - AWS Config設定（リソース監査）
   - バックアップ保持期間延長（30日）
   - アクセスログ保存（ALB, CloudFront, S3）

3. **脅威検知** (所要時間: 3時間)
   - GuardDuty有効化
   - Security Hub統合
   - VPC Flow Logs有効化

4. **運用品質向上** (所要時間: 4時間)
   - Sentry統合（エラー追跡）
   - Performance Insights有効化
   - CloudWatch Dashboards拡充

**合計移行時間: 約21時間（2-3日間）**

---

## 📊 フェーズ別比較表

### アーキテクチャ比較

| コンポーネント | フェーズ1 | フェーズ2 | フェーズ3 |
|------------|----------|----------|----------|
| **ECS タスク数** | 2固定 | 2-6（オートスケール） | 4-10（オートスケール） |
| **ECS スペック** | 0.25vCPU/0.5GB | 0.5vCPU/1GB | 1vCPU/2GB |
| **Aurora ACU** | 0.5固定 | 0.5-2（オートスケール） | 1-4（オートスケール） |
| **Aurora AZ** | Multi-AZ | Multi-AZ | Multi-AZ + 暗号化 |
| **Redis** | なし | Socket.io時追加 | Socket.io時t4g.medium × 2 |
| **CDN** | なし | CloudFront | CloudFront + WAF + Shield |
| **監視** | Logs | Logs + Alarms + X-Ray | 全監視 + Sentry |
| **セキュリティ** | 最低限 | 標準 | フル対応 |
| **バックアップ** | 7日 | 7日 | 30日 + 月次 |

### コスト比較

| フェーズ | 月額コスト（USD） | 月額コスト（JPY） | セラー数 | 物流件数/日 |
|---------|----------------|----------------|----------|------------|
| **フェーズ1** | $147.5 | ¥22,125 | 10名 | 50件 |
| **フェーズ2** | $299 | ¥44,850 | 30名 | 150件 |
| **フェーズ3（Redis無）** | $679 | ¥101,850 | 100名 | 300件 |
| **フェーズ3（Redis有）** | $759 | ¥113,850 | 100名 | 300件 |

### セラー1人あたりコスト

| フェーズ | セラー1人あたり月額 | 物流1件あたりコスト |
|---------|----------------|----------------|
| **フェーズ1** | ¥2,213 | ¥443 |
| **フェーズ2** | ¥1,495 | ¥299 |
| **フェーズ3（Redis無）** | ¥1,019 | ¥340 |
| **フェーズ3（Redis有）** | ¥1,139 | ¥380 |

**重要な洞察**: 
- フェーズ1はMulti-AZ優先で可用性重視
- フェーズ3では規模の経済が働き、セラー1人あたりコストが最も低くなる
- Redis追加はSocket.io実装時のみ

---

## 🛣️ 移行ロードマップ

### タイムライン

```
現在
  ↓
┌─────────────────────────────────────┐
│ フェーズ1: MVP（2-3ヶ月運用）        │
│ - 初期構築: 1週間                    │
│ - 運用学習: 2-3ヶ月                  │
│ - コスト: ¥16,095/月                 │
└─────────────────────────────────────┘
  ↓ セラー20名 or 物流100件/日到達
┌─────────────────────────────────────┐
│ フェーズ2: スケール（3-6ヶ月運用）    │
│ - 移行作業: 1-2日                    │
│ - 性能検証: 1週間                    │
│ - 運用安定化: 3-6ヶ月                │
│ - コスト: ¥50,850/月                 │
└─────────────────────────────────────┘
  ↓ セラー50名 or 外部セラー開始
┌─────────────────────────────────────┐
│ フェーズ3: エンタープライズ（長期）   │
│ - 移行作業: 2-3日                    │
│ - セキュリティ監査: 1週間            │
│ - 本格運用開始                       │
│ - コスト: ¥113,850/月                │
└─────────────────────────────────────┘
  ↓
継続的改善
```

### 各フェーズの成功指標

#### フェーズ1成功指標（2-3ヶ月で評価）

- [ ] サービスが安定稼働（稼働率 > 95%）
- [ ] セラー10名が問題なく利用
- [ ] 物流50件/日を処理
- [ ] P95レスポンスタイム < 3秒
- [ ] 重大な障害発生なし

#### フェーズ2成功指標（3-6ヶ月で評価）

- [ ] サービスが高可用性で稼働（稼働率 > 99%）
- [ ] セラー30名が問題なく利用
- [ ] 物流150件/日を処理
- [ ] P95レスポンスタイム < 2秒
- [ ] ピーク時の性能劣化なし
- [ ] Multi-AZ構成で障害復旧時間 < 1分

#### フェーズ3成功指標（長期運用）

- [ ] サービスが企業品質で稼働（稼働率 > 99.5%）
- [ ] セラー100名が問題なく利用
- [ ] 物流300件/日を処理
- [ ] P95レスポンスタイム < 1.5秒
- [ ] セキュリティ監査合格
- [ ] コンプライアンス要件充足
- [ ] 脅威検知が正常動作（GuardDuty）

---

## 🔧 技術的実装の注意点

### 全フェーズ共通の不変要素

以下のサービス選定は**全フェーズで変更なし**:

1. **ECS Fargate**: 柔軟性・拡張性に必須
2. **Aurora MySQL Serverless v2**: Prisma互換性、自動スケール対応（担当エンジニアのMySQL習熟度を優先）
3. **RDS Proxy**: 接続プーリングに必須

**条件付き要素:**
4. **ElastiCache Redis**: Socket.io実装時のみ必須（フェーズ1では不要）

**データベース選定理由**: PrismaはPostgreSQL/MySQL両対応ですが、担当エンジニアのMySQL習熟度を優先してAurora MySQL Serverless v2を採用します。

### フェーズ1の実装方針

```typescript
// ポーリング方式（30秒間隔）
// lib/hooks/useProductStatus.ts
export function useProductStatus(productId: string, interval = 30000) {
  const [status, setStatus] = useState<string | null>(null);

  useEffect(() => {
    async function fetchStatus() {
      const res = await fetch(`/api/products/${productId}/status`);
      const data = await res.json();
      setStatus(data.status);
    }
    
    fetchStatus();
    const intervalId = setInterval(fetchStatus, interval);
    return () => clearInterval(intervalId);
  }, [productId, interval]);

  return { status };
}
```

フェーズ1ではSocket.io未実装のため、ポーリング方式で状態監視を実装します。

### Prisma接続プーリング設定（MySQL）

```typescript
// lib/db.ts - 全フェーズ共通
import { PrismaClient } from '@prisma/client'

const globalForPrisma = global as unknown as { prisma: PrismaClient }

export const prisma =
  globalForPrisma.prisma ||
  new PrismaClient({
    datasources: {
      db: {
        url: process.env.DATABASE_URL, // RDS Proxy経由（MySQL接続）
      },
    },
    log: ['error', 'warn'],
  })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

**MySQL設定のポイント**:
- PrismaはMySQL 5.7以降をサポート（Aurora MySQL 8.0を使用）
- RDS Proxyを使用することで、107本のAPIルートからの接続を集約
- `schema.prisma`のdatasourceを`provider = "mysql"`に設定

```prisma
// schema.prisma
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
```

---

## 💡 推奨事項

### 初期構築時

1. **フェーズ1で開始**: いきなりフルスペックにしない
2. **IaCで構築**: AWS CDKで全フェーズの構成をコード化
3. **監視を最初から**: CloudWatch Logsは最低限必須
4. **バックアップテスト**: 初期段階でリストア手順を確認

### 運用開始後

1. **メトリクス記録**: フェーズ2移行判断のためのデータ収集
2. **コスト監視**: 月次でコストレビュー
3. **性能テスト**: 定期的に負荷テストを実施
4. **移行リハーサル**: フェーズ2移行前に検証環境でテスト

### やってはいけないこと

1. ❌ Multi-AZ無効化（可用性を犠牲にしない）
2. ❌ RDS Proxyを削除しない（技術的に必須）
3. ❌ フェーズをスキップしない（段階的成長が重要）
4. ❌ 監視を完全にゼロにしない（最低限CloudWatch Logsは必要）

### フェーズ1での柔軟な判断

1. ✅ Redis省略可能（Socket.io未実装のため）
2. ✅ ポーリング方式採用（30秒間隔で業務上問題なし）

---

## 📝 まとめ

### 段階的アプローチの利点

1. **可用性優先**: ¥22,125/月でMulti-AZ対応（99.9%可用性）
2. **コスト最適化**: Redis不要で¥1,500削減
3. **リスク最小化**: 小規模で運用を学んでからスケール
4. **柔軟な拡張**: ビジネス成長に合わせて段階的に機能追加

### 技術的確実性

- ECS Fargate + Aurora MySQL Serverless v2の選択は全フェーズで不変
- フェーズ1ではRedis不要（Socket.io未実装のため）
- RDS Proxyは接続数管理のため全フェーズで必須
- ポーリング方式（30秒間隔）で状態監視実装

### 次のステップ

1. **フェーズ1のCDKコード作成**: 1週間で構築可能
2. **開発環境で検証**: 本番前にテスト
3. **本番デプロイ**: 初期セラー10名で運用開始
4. **2-3ヶ月運用**: メトリクス収集、運用ノウハウ蓄積
5. **フェーズ2移行判断**: 成長指標を基に判断

---

**最終推奨**: フェーズ1（Multi-AZ + Redis無し）で開始し、ビジネスの成長に合わせて段階的にフェーズ2、フェーズ3へ移行することで、**可用性・コスト効率・技術的確実性の三位一体**を実現できます。
